server:
  port: 8080

nodejs:
  url: https://chat-service-social-media.onrender.com

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui
    tags-sorter: alpha
    operations-sorter: alpha

spring:
  cloud:
    gateway:
      mvc:
        routes:
          - id: nodejs-chat-service
            uri: ${nodejs.url}
            predicates:
              - Path=/chat/**
            filters:
              - StripPrefix=1 #hàm .replace("/chat", "")
  datasource:
    url: "jdbc:mysql://mysql-783bdf2-socialmedia-gr10.i.aivencloud.com:15495/SocialMedia?serverTimezone=Asia/Ho_Chi_Minh&ssl-mode=REQUIRED"
    username: avnadmin
    password: AVNS__cZFTBZi9dYoz1fRlYX
  jpa:
    hibernate:
      ddl-auto: validate
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl #Giai quyet van de BaiViet → bai_viet
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true

jwt:
  signerKey: 61b8e4c9638f759c9112471bdfff29f02310f0e6e1053fa9c04d924d3d9869fc
  valid-duration: 3600
  refreshable-duration: 360000
